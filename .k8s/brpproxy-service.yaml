apiVersion: v1
kind: Service
metadata:
  name: brpproxy
  labels:
    app: haal-centraal-brp-bevragen-api
spec:
  selector:
    name: brpproxy
  type: LoadBalancer
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
