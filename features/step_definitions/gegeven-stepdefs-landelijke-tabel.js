const { Given } = require('@cucumber/cucumber');

Given(/^landelijke tabel '([\w-]*)' heeft de volgende waarden$/, function (aanduiding, dataTable) {
    // doe niets, dit is alleen voor documentatie
});
